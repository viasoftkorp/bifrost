- feat: `allowed_models_patterns` and `blacklisted_models_patterns` on virtual key provider configs and `models_patterns` and `blacklisted_models_patterns` on provider keys, stored in new nullable columns (`add_key_model_patterns_json_columns`, `add_vk_provider_config_model_patterns_columns`), included in config hashes only when set, aggregated per provider in the model catalog, and honoured by grant evaluation and key selection
